import { NextResponse } from "next/server";
import { getAuthContext, jsonError, statusForDatabaseError } from "@/lib/api";
export async function PATCH(request:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;const context=await getAuthContext();if(!context.supabase)return jsonError(context.error,503);if(!context.user||context.profile?.role!=="admin")return jsonError("admin_required",403);const body=(await request.json().catch(()=>null))as{status?:string;reason?:string}|null;if(!body?.status)return jsonError("validation_error",422);const{error}=await context.supabase.rpc("admin_resolve_report",{p_report_id:id,p_status:body.status,p_reason:String(body.reason??"")});if(error)return jsonError(error.message,statusForDatabaseError(error.message));return NextResponse.json({status:body.status});}
