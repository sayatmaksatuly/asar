import type { Metadata } from "next";
import { Alert } from "@/components/ui/primitives";
import { getDictionary, isLocale, type Locale } from "@/lib/i18n";
export async function generateMetadata({params}:{params:Promise<{locale:string}>}):Promise<Metadata>{const {locale}=await params;const safe:Locale=isLocale(locale)?locale:"ru";return{title:getDictionary(safe).legal.privacyTitle};}
export default async function PrivacyPage({params}:{params:Promise<{locale:string}>}){const {locale:raw}=await params;const locale:Locale=isLocale(raw)?raw:"ru";const d=getDictionary(locale);return <section className="section"><article className="shell max-w-4xl"><p className="section-kicker">ASAR 1.0</p><h1 className="section-title">{d.legal.privacyTitle}</h1><p className="section-lead">{d.legal.privacyIntro}</p><div className="mt-8"><Alert tone="warning">{d.legal.reviewNotice}</Alert></div><div className="mt-8 grid gap-5">{d.legal.privacySections.map((section)=><section className="content-card" key={section.title}><h2 className="text-2xl font-extrabold">{section.title}</h2><p className="mt-3 leading-7 text-[var(--muted)]">{section.text}</p></section>)}</div></article></section>}
