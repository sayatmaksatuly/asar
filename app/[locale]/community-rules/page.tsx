import type { Metadata } from "next";
import { Alert } from "@/components/ui/primitives";
import { getDictionary, isLocale, type Locale } from "@/lib/i18n";
export async function generateMetadata({params}:{params:Promise<{locale:string}>}):Promise<Metadata>{const {locale}=await params;const safe:Locale=isLocale(locale)?locale:"ru";return{title:getDictionary(safe).legal.rulesTitle};}
export default async function RulesPage({params}:{params:Promise<{locale:string}>}){const {locale:raw}=await params;const locale:Locale=isLocale(raw)?raw:"ru";const d=getDictionary(locale);return <section className="section"><article className="shell max-w-4xl"><p className="section-kicker">ASAR 1.0</p><h1 className="section-title">{d.legal.rulesTitle}</h1><div className="mt-8 grid gap-5"><section className="content-card"><h2 className="text-2xl font-extrabold">{d.common.allowed ?? d.legal.rulesTitle}</h2><p className="mt-3 leading-7 text-[var(--muted)]">{d.legal.rulesAllowed}</p></section><section className="content-card"><h2 className="text-2xl font-extrabold">{d.common.prohibited ?? d.legal.rulesTitle}</h2><ul className="mt-4 list-disc space-y-2 pl-6 text-[var(--muted)]">{d.legal.rulesForbidden.map((item)=><li key={item}>{item}</li>)}</ul></section><Alert tone="warning">{d.legal.rulesSafety}</Alert></div></article></section>}
